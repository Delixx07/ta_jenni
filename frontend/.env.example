# Salin ke .env (lokal) atau set di dashboard Vercel (produksi).

# URL backend. Kosongkan di lokal (pakai proxy Vite).
# Di produksi isi dengan URL backend, mis:
# VITE_API_BASE_URL=https://rcms-backend.up.railway.app
VITE_API_BASE_URL=
