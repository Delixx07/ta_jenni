# ── Web framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── Database / ORM / Migration ─────────────────────────────
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9         # driver PostgreSQL

# ── Konfigurasi & validasi ─────────────────────────────────
pydantic[email]==2.9.2        # [email] menambah email-validator untuk EmailStr
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ── Autentikasi & keamanan ─────────────────────────────────
bcrypt==5.0.0                 # hashing password (dipakai langsung, tanpa passlib)
python-jose[cryptography]==3.3.0   # encode/decode JWT
python-multipart==0.0.9       # form-data (login & upload file)

# ── Ekspor Laporan (Fase 6) ────────────────────────────────
reportlab==4.2.2              # generate PDF
openpyxl==3.1.5              # generate Excel (.xlsx)

# ── Integrasi (Fase 5) ─────────────────────────────────────
apscheduler==3.10.4           # scheduler in-app untuk pengingat
google-auth==2.34.0           # OAuth2 Google
google-auth-oauthlib==1.2.1   # alur OAuth2 (flow)
google-api-python-client==2.143.0  # Google Calendar API
requests==2.32.3              # HTTP client (gateway WA bila dipakai)

# ── Smart Matchmaking (SBERT) ──────────────────────────────
# Dipakai mulai Fase 2. Dicantumkan agar environment konsisten sejak awal.
sentence-transformers==3.1.1
scikit-learn==1.5.2
numpy==1.26.4
